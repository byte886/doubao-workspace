# 全局工作细则（AGENTS.md）

**定位**：豆包工作区的"规则细则手册"。短而硬、每轮都要遵守的规则内核在豆包「设置 → 工作任务偏好指令」（平台每轮自动注入）；载体地图、AI 冷启动顺序、自建技能清单由 `README.md` 唯一维护；本文件只放机制、流程、参数等长细则，由偏好指令"细则下钻"指针触发、AI 主动 Read，**平台不保证自动注入本文件**（2026-09-14 实测：在 `~/Doubao/` 树下开对话，自动注入的是偏好指令而非本文件）。同一内容只在一个载体定义，本文件不复制 README / PROFILE 的内容（DRY）。

**冲突仲裁顺序**（后者服从前者）：**当次对话指令 > 项目文件夹 AGENTS.md（仅该项目内）> 工作任务偏好指令（内核）> 本文件（细则）> PROFILE.md > 豆包自带记忆**。

**变更原则**：默认只增不删；允许优化既有表述，凡重大变化（改默认行为、改结构、删改旧条文）必须在当次交付说明中向用户列清"做成了什么"，本文件内不保留变更日志。

最后更新：2026-09-15

***

## 一、跨会话记忆机制（长效，核心）

解决"每次新对话豆包失忆"。载体分工见 README 第一节、冷启动读取顺序见 README 第三节；本章只定记忆的写入、校准与退役机制。

### 1.1 读取与恢复

1. 不依赖对话记忆：关键事实从白盒文件获取，找不到就如实说"没有记录"，不猜测、不用训练印象补。
2. 若用户要"恢复/继续某次协作机制建设"，先到飞书 AI ＞豆包＞「协作机制与会话档案」容器找对应档案，按其"一分钟恢复指南"点名的本地文件接手（归档与档案结构见第二章）。

### 1.2 写入协议（任务收尾时执行）

**准入标准（同时满足至少一条才写 PROFILE）**：① 跨会话仍有效；② 跨任务可能复用；③ 用户明确要求记住。可入记忆的内容类型：事实、偏好、**约束**、事件。一次性过程细节（临时报错、单次任务步骤）不进长期记忆，归任务报告或日志。

**反向出口（记忆不装什么）**：反复验证有效的*操作流程/做法*不属于记忆，应沉淀为 Skill（自建技能统一放 `~/Doubao/skills/`，唯一根目录；官方技能目录不放个人产物）或项目 WORKFLOW 文档；记忆只存"是什么/要什么"，技能库存"怎么做"。

**架构红线（记忆机制不做成技能）**：规则每轮自动注入、技能仅任务匹配时加载；记忆协议若做成技能，未命中匹配的对话就会漏载，因此它只放规则层与白盒文件，不进 skills/。

* **自建技能平台标注（强制，2026-09-10 起）**：每个 `~/Doubao/skills/<技能>/SKILL.md` 必须在 frontmatter 写 `compatibility` 字段、正文一级标题下写《## 平台适用（执行前先读）》小节。新建/改造技能时：① 写明实际验证过的平台，未验证平台不得默认可跑，Windows 等需另行适配；② 技能执行流程第一步先 `uname -s` 判平台，非已验证平台就停下告知用户"该平台需先适配"，不用想当然的等价命令硬跑；③ 将来补齐 Windows/Linux 后，仍保留"先判平台 → 按平台分流"的结构，mac/Windows 的命令与路径分开写、分别标注验证状态。存量技能的平台状态以各自 frontmatter 为准，不凭印象断言。

**四分类**（沿用业界成熟分法）：

* `user`：用户身份、环境、长期偏好
* `feedback`：**用户对 AI 的纠正**（"不要 X，要 Y"）—— 价值最高，必须同时记下原因和正确做法
* `project`：项目索引（项目名｜路径｜一句话状态｜更新日期），一行一个
* `reference`：外部资源位置（仓库、空间、域名、订阅、面板入口）

**格式与动作**：每条带 `[YYYY-MM-DD]`；写入前用一句话告知用户"我准备记一条什么"，用户说"不用记"则不写；任务收尾发现多条可合并一次说明。

### 1.3 校准与退役

* 发现 PROFILE 或既有记忆过时/矛盾，**当场更新**，问题驱动不留到以后。
* 豆包自带「记忆」仅作模糊补充（只吸收对话文本，有容量上限）；若与白盒文件冲突，以白盒为准，并提醒用户可在「设置 - 记忆」面板清理平台侧旧记忆。冲突仲裁顺序见文首。
* 过时条目标注"[已退役 YYYY-MM-DD：原因]"，不硬删（历史可溯）。

### 1.4 体量红线与演进

* PROFILE.md 目标控制在 **200 行内**（规则/记忆文件过长会降低遵守度）。
* 超线时拆分为 `~/Doubao/memory/` 目录：一个索引文件（只放一行一条的指针）+ 单主题文件，**拆分方案先向用户说明再动**。
* **逐级沉淀（原子 → 模式）**：PROFILE 里同主题的原子条目（如多次同类 feedback）经反复验证后，在任务收尾或「更新记忆」时归纳为 user 区的一条稳定模式，注明归纳自哪些日期，再精简/退役被合并的原子条目；不做后台自动蒸馏，全部人工、可见、可控。
* **指针层与文档网络**：PROFILE 的 project/reference 只放一行指针（路径/链接），细节留在项目文档与飞书；文档之间互链用相对路径或链接、沿链接下钻，同一内容只维护一处（DRY），不复制进记忆。
* **项目级结构化知识/外置记忆用 okf-wiki 技能**：项目内部大量、会复利的知识（架构、模块、决策、知识点）不进 PROFILE，用**本地技能目录** `~/Doubao/skills/okf-wiki/`（直接读本地文件，**它不是飞书空间、勿去飞书搜同名**；OKF v0.2：schema=项目 AGENTS、index 导航、log 时间线、concept 知识点）组织为本地 bundle；新会话按其 `reference/memory-handoff.zh.md` 的 schema→index→log→concept 顺序恢复，PROFILE 只留该项目一行指针。
* 不回溯原则：不主动考古、整理历史信息；只记今后任务中自然产生的有效事实。
* **记忆框架选型结论（2026-09 经 Google/Brave/GitHub 两轮调研；完整调研过程与参考链接见飞书档案《豆包全局长效记忆机制落地（2026-09）》，本处只留结论防止重复评估）**：业界三派——① 重量框架（mem0、Zep/Graphiti、Letta、cognee、TencentDB-Agent-Memory）：需起服务/数据库、经 API Proxy 接入且只支持可改 API base 的开发者客户端，豆包封闭桌面端接不进，团队 ACL/多 Agent 装配对单人无价值，**已排除，不再重复评估**；② 文件记忆派（Claude Code auto memory、Cline Memory Bank、Codex CLI）：纯 Markdown、人可见可控，**本体系采用其思想**；③ 平台自带记忆：只作模糊补充，精确事实走白盒（边界见 1.3）。选定路线即偏好指令 + 本文件 + README + PROFILE 的白盒方案；将来需要定时固化时再评估 cron，不主动上服务端记忆框架。

### 1.5 用户口令

* 用户说"**更新记忆**"：立即按 1.2 把本轮该沉淀的内容写入 PROFILE 并汇报写了什么。
* 用户说"**读记忆**"：重新读取 PROFILE，口头汇报其中要点。
* 用户说"忘掉/删掉 X 记忆"：从 PROFILE 移除或标退役，并说明。

***

## 二、知识库归档细则

> 归档触发原则（什么成果该问、什么不问）在偏好指令第一章，本章只定流程与细则。

1. **流程**：产出可复用成果 → 主动询问"是否放入飞书知识库？" → 用户确认 → 按 `~/Doubao/PROFILE.md` 第 4.1 节「飞书知识空间路由表」归位 → 完成后回告文档标题与链接。**路由表是空间清单的唯一维护处，空间增减只改 PROFILE，其他载体不复制清单。**

2. **空间口径**：一律以路由表现名为准（「学习AI」即「AI」空间，飞书侧已更名）；用户指定位置时按指定；路由表查不到或拿不准归哪个空间时**先问，不默认塞「AI」**；内容含隐私或敏感信息时先确认再归档。

3. **询问行为口令**："不要问了，直接放"= 本次起直接归档不再逐项询问；"以后不用问了"= 调整询问行为；"以后不用问了，不归档"= 关闭归档询问。

4. **协作机制与会话档案（专用容器）**：当任务产出属于"AI 协作机制怎么建、会话如何跨对话恢复"这类元方法/过程档案时，固定归到飞书「AI」空间 ＞「豆包」＞「协作机制与会话档案」节点下（node_token `Qt28wBzoKiPaECk1V0ZcnSNsnbh`），在其下建子文档，不另建位置。档案必须写成新会话可直接接手的形态：① 开头"一分钟恢复指南"，点名要读的本地文件与顺序；② 问题与边界；③ 关键决策（为什么这么定）；④ 落地物清单（路径 + 状态）；⑤ 待办/暂缓项；⑥ 参考来源。建完后在容器索引页表格追加一行（主题/时间/状态/链接）。反过来，当用户要"恢复/继续某次机制建设"，先到该容器找对应档案，按其恢复指南打开本地文件接手，以磁盘现版为准。

***

## 三、搜索技术参数

> 渠道选择原则（连通性为准、专业领域优先权威库）在偏好指令第二章，本章只定技术参数。

1. 代理在本机走 `127.0.0.1:<端口>`，但**端口不写死**：两台机器、ClashX 与 Clash Verge 两类客户端的默认/实际端口本就不同，也可能被手动改过。每次先探测当前客户端的 `mixed-port`/`http-port`/`socks-port`（探测与切换是 mac-system-toolkit 的代理能力，各机客户端与常态端口台账在 dual-machine-manager），再用探到的端口做连通性自测：`curl -x http://127.0.0.1:<端口> --max-time 5 https://www.google.com`；超时即判不可用，改用国内渠道（百度、必应国内版、专业数据库等），不反复重试、不强访不可用资源。
2. 代理只解决"可达性"，不改变凭证红线；代理的安全边界（可信通道、HTTPS、DNS 泄漏/Kill Switch、订阅地址本身属敏感凭证）下钻 security-baseline 技能（按其索引找网络篇）；代理开关与端口探测的**动作**是 mac-system-toolkit 的代理能力，本文件不复制。

***

## 四、自建技能规范

1. **唯一根目录**：`~/Doubao/skills/`；官方技能在应用内部目录，不放个人产物。技能只存"怎么做"，事实与偏好不进技能。
2. **目录名 = 技能名**：技能目录名必须与 `SKILL.md` frontmatter 的 `name` 一致。
3. **平台标注**：按 1.2「自建技能平台标注（强制）」执行，动手前先 `uname -s` 判平台。
4. **开发中技能**：允许暂带 PRD/ROADMAP/phase spec/mcp-server 等研发文档，技能稳定后再剥离纯项目材料；哪个技能在开发以 README 清单标注为准。
5. **清单唯一维护处**：技能清单只在 README 第四节维护（数量以该处为准，本文件不写数字以免过时），本文件不复制；新增、更名或状态变化后同步更新该处。
6. **路径可移植（双机同步硬要求）**：同一套 `~/Doubao` 在两台 Mac（用户名 chenwenjie、wenjiechen，git 提交统一在 wenjiechen 机执行）间同步。技能与脚本内**禁止硬编码 `/Users/<用户名>`**：Shell 用 `$HOME`、Python 用 `Path.home()`、文档示例用 `~`；注意**引号内和 MCP/GUI 配置框内 `~` 不展开**，这类位置用 `$HOME` 或注明先 `echo ~/...` 取绝对路径。`third-party/`、`target/`、`venv/` 等第三方源码与构建产物不手改（重新构建自动适配）；OKF bundle 里的 `human:<id>` 是作者标识不是路径，保持稳定不改。

***

## 五、文档与知识治理（单一事实源）

> 载体级分工（README / PROFILE / AGENTS / skills 各管什么）见文首与 README；本章定"同一段内容不重复写"的内容级规则，全局载体、自建技能、项目文档一律适用。

1. **会变的内容只在唯一权威源写一次**：算法、参数、流程、清单、定义这类"一处改、处处得对"的内容，只维护一份；其他文件用到时只放**相对指针**（链接 ＋ 一句"为何读"），不复制正文。复制即双写，权威源一变副本先过时、反而误导（2026-09-14 凭证规则在三个文件各复述一遍、改一处要追三处的教训）。
2. **三类不算重复，就近保留**：① 导航 / 目录 / 清单（路标本就是 README 的职责）；② 本模块的**功能特有语义**（权威源里没有、只属此处的风险与约束）；③ **AI 即时行为红线**（必须在入口当场可见、不能靠跳转才生效的强制指令）。
3. **判据**：问自己"这段变了要不要同步改多处？"——要，就抽成单一源 ＋ 指针；只在此处生效，可就地写；拿不准就指针化。
4. **新建持久文档 / 机制前先查权威源**：逐条列出它要承担的职责，若已被现有文件承担则**不新建**，改在权威源补内容，防重复建设。
5. **反复验证、跨项目复用的"做法"沉淀为技能而非规则条文**：通用方法论做成 `~/Doubao/skills/` 下独立技能（按需加载），各项目只留"项目特有约定 ＋ 指向技能的指针"，不把通用方法抄进每个项目（同 idea-to-tickets 的处理）。

***

## 六、安全基线与凭证（纯指针，正文不展开）

安全与凭证是横切能力，**统一按 `skills/security-baseline/` 执行；本文件与 PROFILE 只挂指针、不复制条文**（单一事实源，避免改一处要追多处）。

- 命中"密码/主口令/凭证存哪、密码管理器、token/密钥/私钥、能否进公开仓、泄漏应急、打码脱敏、最小权限、VPN 安全"等任一情形，先 Read `skills/security-baseline/SKILL.md`，再按其按需索引下钻对应 references。
- 加解密、明文巡检、代理开关等**动作**：mac-system-toolkit（命令 `secrets`、`audit-secrets.sh`，以及代理开关能力）。
- 两台机器**具体**有哪些凭证、落点、账号、代理客户端与端口等**台账事实**：dual-machine-manager。
- 每轮在场的最高原则只点一句：明文密钥不进 git/日志/对外材料、人只记 1 个主口令、怀疑泄漏先轮换；细则、强度标准与清单一律以技能正文为准。

***

## 七、横切治理技能路由（双机运维 / 项目管理）

安全路由见第六章。另两个与 security-baseline 平级的全局横切治理技能，命中即 Read 其 `SKILL.md` 再按索引下钻，规则层只挂指针、不复制方法论：

- **dual-machine-manager（双机管家）**：涉及两台 Mac、`ssh wj`/`ssh cw`、双机 git 同步（对端只 ff-only、禁 pull）、launchd/brew/cron 后台服务、远程关机、OpenToken、双机硬件型号/软件/包管理/凭证落点台账、日常巡检与双机差异一致性时读它。
- **project-manager（项目管理）**：新方向立项（Project Charter/目录模板）、多项目组合盘点与优先级、健康灯、活跃项目 7±2 纪律、阶段门 G1/G2/G3、项目收尾退役（退役清单须含凭证吊销/轮换，对接第六章）时读它。
- **三者分工与关系**：security-baseline 定"安全规矩与凭证来处"（脑）、mac-system-toolkit 提供加解密/巡检/代理等"动作"（手）、dual-machine-manager 记"双机台账与运维"（账）、project-manager 管"项目生命周期"。业务项目内只留项目特有约定 + 指向这些全局技能的指针，不把通用方法抄进每个项目（DRY，见第五章）。

### 7.1 指针方向约定（要逻辑闭环，不要双向循环）

技能/载体之间的"关联指针"必须是**有向无环**的，目标是"从任务进来一路向下能把相关能力串齐、最后回到任务把事做完"，而不是 A 指 B、B 又指回 A 的踢皮球：

- **层次序（箭头只允许自上而下 / 沿任务主流程向前）**：
  L0 偏好指令 → L1 全局载体（README 导航 / AGENTS 路由 / PROFILE 事实）→ L2 流程方法（project-manager）、横切规矩（security-baseline）→ L3 执行与台账（mac-system-toolkit、dual-machine-manager、各业务技能）→ L4 具体业务项目。
- **同层只允许"流程步骤联动"的单向边**：如 project-manager 在立项/退役步联动 security-baseline、dual；被联动方**不写回指链接**，只可用一句不带跳转的话陈述自身边界（"X 不属本篇"，不写"X 见某技能"）。
- **下层不回指上层**：dual / mac-toolkit 等执行层不写"方法论见 security-baseline"这类回边；"做下层任务前先加载哪条上层规矩"由 L1 入口（本章路由）规定加载顺序来保证，例如命中双机凭证时：AGENTS 路由 → 先 security-baseline（规矩）→ 其内部点名去 dual（台账）/ mac-toolkit（动作），单向不回头。
- **判据**：任意两个节点之间只保留一条方向的边；若发现两边互相点名跳转，删掉回边，改为被指方的"无链接边界陈述"，加载顺序问题交回入口路由。业务项目（叶子）指向全局技能不算回边，因为全局技能不指回具体项目。
- **能力依赖，不位置耦合（解耦）**：技能 A 需要技能 B 时，只声明"我需要什么能力"（可点 B 的**技能名**），**不写 B 的内部文件路径**（不写 `B/references/xxx.md`、`B/scripts/xxx.sh`）；B 内部文件怎么拆分、改名、导航，是 B 自己 SKILL 索引的事，A 不跟着改。技能里只保留两类"位置"：① 指向**自身** references 的内部导航；② 稳定的**调用接口/命令名**（如全局命令 `secrets`、`audit-secrets.sh`、`gita`——这是要执行的接口，不是对方文档位置）。
- **位置知识只集中在入口层**："哪个能力在哪个技能、入口文件是什么"只在 README 技能清单与 AGENTS 路由维护一份，且只到**技能级**（点名技能 / 其 SKILL.md），不深入到对方 references 文件；技能正文不再重复维护"对方文件在哪"（即中心注册表/registry 解析依赖，而非模块互相硬指）。
- **区分两张图（无环依赖原则 ADP 的适用边界）**：技能的"加载/执行依赖图"（agent 真正按什么顺序 Read/调用谁）必须是 DAG、不得有环——成环的模块无法独立改动、会互相牵连（domino）；而"概念关联"（正文用纯文本提一句"相关能力还有 X"）不是加载边、不被执行，允许双向提及，需要时能联动即可（类比笔记软件的双向链接，它是导航不是依赖）。判据：这句话会不会让 agent 真去加载/调用对方——会，就受 DAG 约束；只是相关性提示，就不算回边。依赖稳定的能力名/命令接口，不依赖易变的对方内部文件路径（依赖抽象不依赖细节）。
