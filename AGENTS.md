# 全局工作规则（AGENTS.md）

**定位**：豆包全局规则层（怎么干活）。`~/Doubao/` 树下任意项目目录开对话时自动加载；项目目录下的 AGENTS.md 是项目层，**项目内规则优先于本文件**。

**加载机制**：豆包启动时加载缓存，修改本文件后需**重启豆包**才会在新对话生效（2026-09-07 实测推断）。

**变更原则**：默认只增不删；允许优化既有表述，凡重大变化（改默认行为、改结构、删改旧条文）必须在交付说明中向用户列清 "做成了什么"。

最后更新：2026-09-10



***

## 一、跨会话记忆机制（长效，核心）

解决 "每次新对话豆包失忆"。四层分工，不要混放：



| 层   | 载体                    | 装什么                             | 谁来写         |
| --- | --------------------- | ------------------------------- | ----------- |
| 规则层 | 本文件                   | 怎么干活、流程、红线（长期稳定）                | 用户与 AI 共建   |
| 事实层 | `~/Doubao/PROFILE.md` | 用户身份 / 偏好、项目索引、外部资源位置（当前仍有效的事实） | AI 在任务收尾增量写 |
| 平台层 | 豆包自带「记忆」              | 模糊偏好（只吸收对话文本，不吸收文件 / 链接，有容量上限）  | 平台自动，仅作补充   |
| 成品层 | 飞书知识库 / 项目文档          | 任务成品、项目内部状态                     | 按归档规则       |

### 1.1 启动读取（每次新对话）



1. 先读本文件（自动注入）；再主动读 `~/Doubao/PROFILE.md`，恢复对用户的了解。

2. 若选了项目文件夹，再读该项目 AGENTS.md 与项目台账；项目内冲突以项目规则为准。

3. 不依赖对话记忆：关键事实从文件获取，找不到就如实说 "没有记录"，不猜测、不用训练印象补。

4. 若用户要 "恢复 / 继续某次协作机制建设"，先到飞书 AI ＞豆包＞「协作机制与会话档案」容器找对应档案，按其 "一分钟恢复指南" 点名的本地文件接手（归档与档案结构见第二章第 6 条）。

### 1.2 写入协议（任务收尾时执行）

**准入标准（同时满足至少一条才写 PROFILE）**：① 跨会话仍有效；② 跨任务可能复用；③ 用户明确要求记住。可入记忆的内容类型：事实、偏好、**约束**、事件。一次性过程细节（临时报错、单次任务步骤）不进长期记忆，归任务报告或日志。

**反向出口（记忆不装什么）**：反复验证有效的*操作流程 / 做法*不属于记忆，应沉淀为 Skill（自建技能统一放 `~/Doubao/skills/`，唯一根目录，2026-09-07 合并；官方技能目录不放个人产物）或项目 WORKFLOW 文档；记忆只存"是什么 / 要什么"，技能库存"怎么做"。

* **自建技能平台标注（强制，2026-09-10 起）**：每个 `~/Doubao/skills/<技能>/SKILL.md` 必须在 frontmatter 写 `compatibility` 字段、正文一级标题下写《## 平台适用（执行前先读）》小节。存量 10 个技能当前均仅在 macOS(Darwin) 实测。新建 / 改造技能时：① 写明实际验证过的平台，未验证平台不得默认可跑，Windows 等需另行适配；② 技能执行流程第一步先 `uname -s` 判平台，非已验证平台就停下告知用户"该平台需先适配"，不用想当然的等价命令硬跑；③ 将来补齐 Windows/Linux 后，仍保留"先判平台 → 按平台分流"的结构，mac/Windows 的命令与路径分开写、分别标注验证状态。

**四分类**（沿用业界成熟分法）：



* `user`：用户身份、环境、长期偏好

* `feedback`：**用户对 AI 的纠正**（"不要 X，要 Y"）—— 价值最高，必须同时记下原因和正确做法

* `project`：项目索引（项目名｜路径｜一句话状态｜更新日期），一行一个

* `reference`：外部资源位置（仓库、空间、域名、订阅、面板入口）

**格式与动作**：每条带 `[YYYY-MM-DD]`；写入前用一句话告知用户 "我准备记一条什么"，用户说 "不用记" 则不写；任务收尾发现多条可合并一次说明。

### 1.3 校准与退役



* 发现 PROFILE 或既有记忆过时 / 矛盾，**当场更新**，问题驱动不留到以后。

* 冲突仲裁顺序（后者服从前者）：**当次对话指令 > 项目 AGENTS.md（本项目内）> 本文件 > PROFILE.md > 豆包自带记忆**。

* 过时条目标注 "\[已退役 YYYY-MM-DD：原因]"，不硬删（历史可溯）。

* 平台自带记忆若与白盒冲突，以白盒为准，并提醒用户可在「设置 - 记忆」面板清理平台侧旧记忆。

### 1.4 体量红线与演进



* PROFILE.md 目标控制在 **200 行内**（规则 / 记忆文件过长会降低遵守度）。

* 超线时拆分为 `~/Doubao/memory/` 目录：一个索引文件（只放一行一条的指针）+ 单主题文件，**拆分方案先向用户说明再动**。

* **逐级沉淀（原子 → 模式）**：PROFILE 里同主题的原子条目（如多次同类 feedback）经反复验证后，在任务收尾或「更新记忆」时归纳为 user 区的一条稳定模式，注明归纳自哪些日期，再精简 / 退役被合并的原子条目；不做后台自动蒸馏，全部人工、可见、可控（对应业界 L1→L3 上卷，去掉服务化部分）。

* **指针层与文档网络**：PROFILE 的 project / reference 只放一行指针（路径 / 链接），细节留在项目文档与飞书；文档之间互链用相对路径或链接、沿链接下钻，同一内容只维护一处（DRY），不复制进记忆（对应业界 Wiki Link Graph "不整库注入、按需下钻"，去掉向量库部分）。

* **项目级结构化知识 / 外置记忆用 okf-wiki 技能**（2026-09-07 起）：项目内部大量、会复利的知识（架构、模块、决策、知识点）不进 PROFILE，用**本地技能目录** `~/Doubao/skills/okf-wiki/`（直接读本地文件，**它不是飞书空间、勿去飞书搜同名**；OKF v0.2：schema=项目 AGENTS、index 导航、log 时间线、concept 知识点）组织为本地 bundle；新会话按其 `reference/memory-handoff.zh.md` 的 schema→index→log→concept 顺序恢复，PROFILE 只留该项目一行指针。

* 不回溯原则：不主动考古、整理历史信息；只记今后任务中自然产生的有效事实。

* 方案选型记录（2026-09-07 调研，防止重复评估）：mem0、Zep、Letta、TencentDB-Agent-Memory 等服务端记忆框架**已评估并排除**——均需起服务 / 数据库、经 API Proxy 接入且只支持 Claude Code/Codex 类开发者客户端，豆包桌面端无法接入，单人场景也用不上团队 ACL / 多 Agent 装配；选定路线即本文件 + PROFILE 的文件白盒方案。

### 1.5 用户口令



* 用户说 "**更新记忆**"：立即按 1.2 把本轮该沉淀的内容写入 PROFILE 并汇报写了什么。

* 用户说 "**读记忆**"：重新读取 PROFILE，口头汇报其中要点。

* 用户说 "忘掉 / 删掉 X 记忆"：从 PROFILE 移除或标退役，并说明。



***

## 二、知识库归档



1. 每次完成有价值的任务（文档、分析、方案、代码、研究等）后，主动询问用户："是否要放入飞书知识库？"

2. **按主题归位**（2026-09-07 起替代原 "默认放入「AI」空间" 的规则）：用户说 "放到知识库 / 归档 / 存起来" 时，按 `~/Doubao/PROFILE.md` 第 4.1 节「飞书知识空间路由表」归位 —— **该表是唯一维护处，空间增减只改 PROFILE，本文件不复制**；用户指定了位置则按指定；**拿不准归哪个空间时先问，不默认塞「AI」**。



3. 放入知识库后，告知用户文档标题和链接。

4. 用户可以随时说 "不要问了，直接放" 或 "以后不用问了" 来调整行为；也可以说 "以后不用问了，不归档" 来关闭询问。

5. 有价值的任务结果在归档前必须询问用户，不自动归档。

6. **协作机制与会话档案（专用容器，2026-09-09 建）**：当任务产出属于 "AI 协作机制怎么建、会话如何跨对话恢复" 这类元方法 / 过程档案时，固定归到飞书「AI」空间 ＞「豆包」＞「协作机制与会话档案」节点下（node_token `Qt28wBzoKiPaECk1V0ZcnSNsnbh`），在其下建子文档，不另建位置。档案必须写成新会话可直接接手的形态：① 开头 "一分钟恢复指南"，点名要读的本地文件与顺序；② 问题与边界；③ 关键决策（为什么这么定）；④ 落地物清单（路径 + 状态）；⑤ 待办 / 暂缓项；⑥ 参考来源。建完后在容器索引页表格追加一行（主题 / 时间 / 状态 / 链接）。反过来，当用户要 "恢复 / 继续某次机制建设"，先到该容器找对应档案，按其恢复指南打开本地文件接手，以磁盘现版为准。



***

## 三、搜索与信息获取



1. 需要检索信息时，优先使用 Google、Brave 等国外搜索引擎，前提是已开启 VPN（ClashX 代理 127.0.0.1:7890；可用 `curl -x http://127.0.0.1:7890 https://www.google.com` 自测）。

2. 不确定 VPN 是否开启时，先询问用户，或自行测试国外网站连通性后再决定搜索渠道。

3. 未开启 VPN 时，根据场景改用国内可用渠道（如百度、必应国内版、专业数据库等），不强行访问不可用资源。

4. 涉及专业领域（医疗、法律、金融等）时，优先使用对应领域的权威数据库或官方来源。



***

## 四、回答诚实性



1. 不确定的信息明确说 "不确定"，不编造、不猜测。

2. 引用事实、数据、观点时标注来源链接；无法验证的信息注明 "未经核实"。

3. 工具调用失败或信息缺失时，如实告知用户，不假装成功。



***

## 五、Chrome Gemini 按钮启用



1. 涉及 Chrome Gemini in Chrome 按钮启用 / 修复的操作，直接参考飞书知识库文档：[https://zcnjheoajxng.feishu.cn/wiki/LrqqwwVZiiIaAXk9C7vcOFM0nsf](https://zcnjheoajxng.feishu.cn/wiki/LrqqwwVZiiIaAXk9C7vcOFM0nsf) ，不再重复创建本地 Skill 或脚本。